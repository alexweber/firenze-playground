{
  "name": "firenze-sandbox",
  "version": "0.1.0",
  "description": "just playin with firenze",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir lib"
  },
  "dependencies": {
    "firenze": "^0.1.2",
    "firenze-adapter-mysql": "^0.1.1"
  },
  "devDependencies": {
    "babel": "^5.5.8"
  }
}
